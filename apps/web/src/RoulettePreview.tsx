type Segment = { id: string; label: string; color: string };
export function RoulettePreview({ segments, backgroundColor }: { segments: Segment[]; backgroundColor: string }) {
  const center = 150, radius = 118, step = (Math.PI * 2) / segments.length;
  return <div className="roulette-preview" style={{ backgroundColor }}><svg viewBox="0 0 300 300" role="img" aria-label="Vista previa de la ruleta"><polygon points="150,8 142,27 158,27" fill="#f3f6f9" />{segments.map((segment, index) => { const start = -Math.PI / 2 + index * step; const end = start + step; const points = [`${center},${center}`, `${center + radius * Math.cos(start)},${center + radius * Math.sin(start)}`, `${center + radius * Math.cos(end)},${center + radius * Math.sin(end)}`].join(' '); const angle = start + step / 2; const x = center + radius * 0.62 * Math.cos(angle); const y = center + radius * 0.62 * Math.sin(angle); return <g key={segment.id}><polygon points={points} fill={segment.color} stroke="#111923" strokeWidth="2" /><text x={x} y={y} textAnchor="middle" dominantBaseline="middle" transform={`rotate(${(angle * 180) / Math.PI + 90},${x},${y})`}>{segment.label}</text></g>; })}<circle cx="150" cy="150" r="18" fill="#111923" stroke="#f3f6f9" strokeWidth="3" /></svg></div>;
}
