import { useMemo, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { apiRequest } from '../../../shared/api/client';
import { commercialApi, type Plan } from '../../commercial/api';
import { experiencesApi } from '../../experiences/api';
import { rouletteTemplates } from '../../roulette/templates';

type Mode = 'later' | 'offline' | 'free' | 'courtesy' | 'configure';
type Created = { organizationId: string; experienceId: string; slug: string; access?: string };

export function ClientOnboardingPage() {
  const navigate = useNavigate();
  const [step, setStep] = useState(1), [organizationName, setOrganizationName] = useState(''), [customerEmail, setCustomerEmail] = useState(''), [customerName, setCustomerName] = useState(''), [customerPassword, setCustomerPassword] = useState(''), [planId, setPlanId] = useState(''), [mode, setMode] = useState<Mode>('configure'), [templateId, setTemplateId] = useState('minimal'), [experienceName, setExperienceName] = useState(''), [segmentCount, setSegmentCount] = useState(6), [plans, setPlans] = useState<Plan[]>([]), [org, setOrg] = useState<{ id: string; name: string }>(), [created, setCreated] = useState<Created>(), [busy, setBusy] = useState(false), [error, setError] = useState('');
  const template = useMemo(() => rouletteTemplates.find((item) => item.id === templateId) ?? rouletteTemplates[0], [templateId]);
  async function createOrganization() { if (!organizationName.trim() || !customerEmail.trim() || !customerName.trim() || customerPassword.length < 8 || busy) return; setBusy(true); setError(''); try { const result = await apiRequest<{ id: string; name: string }>('/admin/organizations', undefined, { method: 'POST', body: JSON.stringify({ name: organizationName.trim() }) }); await apiRequest('/admin/users', undefined, { method: 'POST', body: JSON.stringify({ organizationId: result.id, email: customerEmail.trim(), name: customerName.trim(), password: customerPassword, role: 'admin' }) }); setOrg(result); setCustomerPassword(''); await loadPlans(result.id); setStep(2); } catch (e) { setError((e as Error).message); } finally { setBusy(false); } }
  async function loadPlans(id: string) { try { setPlans(await apiRequest<Plan[]>('/plans', id)); } catch (e) { setError((e as Error).message); } }
  async function createExperience() { if (!org || !experienceName.trim() || busy) return; setBusy(true); setError(''); try { const config = { ...template.config, segments: template.config.segments.slice(0, Math.max(6, Math.min(10, segmentCount))) }; const experience = await experiencesApi.create(org.id, { name: experienceName.trim(), type: 'roulette', draft_config: config }) as { id: string; slug: string }; setCreated({ organizationId: org.id, experienceId: experience.id, slug: experience.slug }); setStep(4); } catch (e) { setError((e as Error).message); } finally { setBusy(false); } }
  async function createAccess() { if (!org || !created || !planId || mode === 'configure' || busy) return; setBusy(true); try { const subscription = await commercialApi.createSubscription(org.id, { plan_id: planId, experience_ids: [created.experienceId], starts_at: new Date().toISOString() }); setCreated((current) => ({ ...current!, access: subscription.effectiveStatus })); if (mode === 'courtesy') await commercialApi.grant(subscription.id, org.id, { grant_type: 'courtesy', duration_days: 30, note: 'Cortesía creada desde onboarding' }); setStep(5); } catch (e) { setError((e as Error).message); } finally { setBusy(false); } }
  async function publish() { if (!org || !created || busy) return; setBusy(true); try { await experiencesApi.publish(created.experienceId, org.id); setStep(6); } catch (e) { setError((e as Error).message); } finally { setBusy(false); } }
  const selectedPlan = plans.find((plan) => plan.id === planId);
  return <main className="page"><div className="page-heading"><div><p className="eyebrow">ONBOARDING / CLIENTE</p><h1>Nueva activación</h1></div><span>Paso {step} de 6</span></div>{error && <p className="error" role="alert">{error}</p>}{step === 1 && <section className="experience-card"><h2>1. Nuevo cliente</h2><p>La contraseña se usa para generar el hash y no se guarda en texto plano.</p><label>Organización<input autoFocus value={organizationName} onChange={(e) => setOrganizationName(e.target.value)} placeholder="Cliente Evento Norte" /></label><label>Email del cliente<input type="email" value={customerEmail} onChange={(e) => setCustomerEmail(e.target.value)} placeholder="customer@test.local" /></label><label>Nombre del cliente<input value={customerName} onChange={(e) => setCustomerName(e.target.value)} placeholder="Operador Cliente" /></label><label>Contraseña temporal local<input type="password" minLength={8} value={customerPassword} onChange={(e) => setCustomerPassword(e.target.value)} placeholder="password" /></label><button disabled={busy || organizationName.trim().length < 2 || !customerEmail.trim() || !customerName.trim() || customerPassword.length < 8} onClick={() => void createOrganization()}>{busy ? 'Creando…' : 'Continuar'}</button></section>}{step === 2 && <section className="experience-card"><h2>2. Plan y modalidad</h2><label>Plan<select value={planId} onChange={(e) => setPlanId(e.target.value)}><option value="">Elegí un plan</option>{plans.filter((plan) => plan.pricingMode !== 'unconfigured').map((plan) => <option key={plan.id} value={plan.id}>{plan.name}</option>)}</select></label><label>Modalidad<select value={mode} onChange={(e) => setMode(e.target.value as Mode)}><option value="configure">Solo configurar ahora</option><option value="later">Mercado Pago más adelante</option><option value="offline">Efectivo / transferencia más adelante</option><option value="free">FREE</option><option value="courtesy">Cortesía</option></select></label>{selectedPlan && <p>Capacidades: {selectedPlan.name}</p>}<button disabled={!planId && mode !== 'configure'} onClick={() => setStep(3)}>Continuar</button></section>}{step === 3 && <section className="experience-card"><h2>3. Configuración básica</h2><label>Plantilla<select value={templateId} onChange={(e) => setTemplateId(e.target.value)}>{rouletteTemplates.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><label>Nombre<input value={experienceName} onChange={(e) => setExperienceName(e.target.value)} placeholder="Ruleta Acceptance" /></label><label>Segmentos<select value={segmentCount} onChange={(e) => setSegmentCount(Number(e.target.value))}>{[6, 8, 10].map((count) => <option key={count}>{count}</option>)}</select></label><button disabled={busy || !experienceName.trim()} onClick={() => void createExperience()}>Crear experiencia</button></section>}{step === 4 && <section className="experience-card"><h2>4. Acceso comercial</h2><p>{org?.name} · {experienceName}</p>{mode === 'configure' ? <p>Acceso pendiente de configuración.</p> : <button disabled={busy} onClick={() => void createAccess()}>Crear acceso</button>}<button onClick={() => setStep(5)}>Continuar sin acceso</button></section>}{step === 5 && <section className="experience-card"><h2>5. Publicar</h2><button disabled={busy} onClick={() => void publish()}>Publicar</button><button className="secondary" onClick={() => navigate(`/app/experiences/${created?.experienceId}`)}>Editar avanzado</button></section>}{step === 6 && created && <section className="experience-card"><h2>6. QR listo</h2><p>{`${import.meta.env.VITE_RUNTIME_BASE_URL ?? 'http://localhost:5174'}/r/${encodeURIComponent(created.slug)}`}</p><a className="configure" href={`${import.meta.env.VITE_RUNTIME_BASE_URL ?? 'http://localhost:5174'}/r/${encodeURIComponent(created.slug)}`} target="_blank" rel="noreferrer">Abrir experiencia →</a></section>}</main>;
}
