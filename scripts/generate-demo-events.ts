async function main() { const url=process.env.API_URL??'http://127.0.0.1:8787';const key='cor_app_dev_cosquin_2026';const games=['Ruleta','Trivia','Memotest'];const events=[];for(let i=0;i<300;i++){const user=`anon_${i}`;const game=games[i%3];const t=Date.now()-((i%14)*86400000+(i%24)*3600000);events.push({event:'app_opened',userId:user,occurredAt:t,properties:{game}});events.push({event:'game_started',userId:user,sessionId:`sess_${i}`,occurredAt:t+1000,properties:{game}});if(i%4!==0)events.push({event:'game_finished',userId:user,sessionId:`sess_${i}`,occurredAt:t+2000,properties:{game,result:i%3?'win':'lose'}})}for(let i=0;i<events.length;i+=50){await fetch(`${url}/v1/events/batch`,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${key}`},body:JSON.stringify({events:events.slice(i,i+50)})})}console.log(`Generated ${events.length} events for local development.`); }
void main();
